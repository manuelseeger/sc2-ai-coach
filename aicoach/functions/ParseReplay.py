import typing
from . import FunctionBase


class ParseReplay(FunctionBase):
    '''Parse a replay file and return a list of frames.'''

    def __call__(self, replay_file: str) -> str:
        '''Parse a replay file and return a list of frames.'''
        pass
    
